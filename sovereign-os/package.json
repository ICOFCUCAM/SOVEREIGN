{
  "name": "sovereign-os",
  "version": "0.1.0",
  "private": true,
  "description": "Sovereign AI Media & Acquisition Infrastructure — a three-layer sovereign platform seeded from the migrated Media Acquisition Ecosystem.",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*",
    "layers/layer-2-distribution",
    "layers/layer-2-distribution/worker",
    "layers/layer-3-intelligence",
    "layers/layer-1-media/worker"
  ],
  "scripts": {
    "build": "tsc -b packages/core packages/intel-knowledge-contract packages/intel-knowledge-reference && npm --workspace @sovereign/api-platform run build && npm --workspace @sovereign/distribution run build && npm --workspace @sovereign/intelligence run build",
    "typecheck": "tsc -b packages/core packages/intel-knowledge-contract packages/intel-knowledge-reference --noEmit",
    "test": "npm run build && node --test packages/*/test/*.test.js layers/*/test/*.test.js",
    "dashboard:dev": "npm --workspace @sovereign/command-center run dev",
    "dashboard:build": "npm --workspace @sovereign/command-center run build",
    "portal:dev": "npm --workspace @sovereign/developer-portal run dev",
    "portal:build": "npm --workspace @sovereign/developer-portal run build",
    "web:dev": "npm --workspace @sovereign/web run dev",
    "web:build": "npm --workspace @sovereign/web run build",
    "worker:dev": "npm --workspace sovereign-ffmpeg-worker run start"
  },
  "engines": {
    "node": ">=20"
  }
}
